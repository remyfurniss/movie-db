first run these commands.

git clone https://github.com/remyfurniss/movie-db.git
cd movie-db

using the .env.example files provided, create these files.

backend/.env
frontend/.env

then run this command.

docker compose up --build

visit this url to use the app.

http://localhost:5173